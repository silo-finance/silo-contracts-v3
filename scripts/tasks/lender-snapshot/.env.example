# Copy to `.env` (gitignored) and fill in real values for local runs.
# Never commit real keys.

# Archive RPC endpoint for the target chain (must support eth_call at a historical block).
# Example shape (NOT a real key): https://lb.drpc.live/sonic/<YOUR_DRPC_KEY>
RPC_URL=

# The Graph gateway API key (Bearer token).
THE_GRAPH_API_KEY=
