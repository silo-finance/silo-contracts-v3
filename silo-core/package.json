{
  "name": "silo-core",
  "version": "0.0.0",
  "engines": {
    "node": ">=16.0.0"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:silo-finance/silo-contracts-v3.git"
  },
  "license": "BUSL-1.1",
  "devDependencies": {
    "solhint": "~3.4.1"
  },
  "scripts": {
    "lint:sol": "../scripts/lint-sol.sh 0",
    "lint-tests:sol": "../scripts/lint-sol.sh 0 test/**/*.sol",
    "lint-deploy:sol": "../scripts/lint-sol.sh 0 deploy/**/*.sol",
    "lint-common:sol": "../scripts/lint-sol.sh 0 common/**/*.sol",
    "foundry:qa": "FOUNDRY_PROFILE=core forge test -vvv"
  }
}
